import 'package:flutter/material.dart';

class HomePageLoadedView extends StatelessWidget {
  const HomePageLoadedView({super.key});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
