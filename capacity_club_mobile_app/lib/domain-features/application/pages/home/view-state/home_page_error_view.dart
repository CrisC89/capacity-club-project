import 'package:flutter/material.dart';

class HomePageErrorView extends StatelessWidget {
  const HomePageErrorView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Text('ERROR, please try again'),
    );
  }
}
