import 'package:flutter/material.dart';

class TestPageError extends StatelessWidget {
  const TestPageError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Text('ERROR, please try again'),
    );
  }
}
