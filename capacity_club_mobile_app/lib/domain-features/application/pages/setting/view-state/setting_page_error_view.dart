import 'package:flutter/material.dart';

class SettingPageErrorView extends StatelessWidget {
  const SettingPageErrorView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Text('ERROR, please try again'),
    );
  }
}
