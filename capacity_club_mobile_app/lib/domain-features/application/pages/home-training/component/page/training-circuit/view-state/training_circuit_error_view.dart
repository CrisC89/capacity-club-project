import 'package:flutter/material.dart';

class TrainingCircuitErrorView extends StatelessWidget {
  const TrainingCircuitErrorView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Text('ERROR, please try again'),
    );
  }
}
