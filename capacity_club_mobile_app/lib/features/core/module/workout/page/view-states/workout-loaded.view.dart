import 'package:flutter/material.dart';

class WorkoutLoadedView extends StatelessWidget {
  const WorkoutLoadedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Text('Loaded'),
    );
  }
}
