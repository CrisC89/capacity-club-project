import 'package:flutter/material.dart';

class CollectiveTrainingSessionDetailErrorView extends StatelessWidget {
  const CollectiveTrainingSessionDetailErrorView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Text('DETAIL ERROR, please try again'),
    );
  }
}
